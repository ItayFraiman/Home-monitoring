name: build-sensor-image

on:
  push:
    branches: ["main"]
    paths:
      - "sensor-sim/**"
      - ".github/workflows/build-sensor.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase owner
        id: owner
        run: echo "name=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: ./sensor-sim
          push: true
          tags: |
            ghcr.io/${{ steps.owner.outputs.name }}/home-monitoring/sensor-sim:${{ github.sha }}
            ghcr.io/${{ steps.owner.outputs.name }}/home-monitoring/sensor-sim:main